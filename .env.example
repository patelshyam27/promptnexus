VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-goes-here

# Create a local `.env` or `.env.local` with these values.
# Keep keys secret; do not commit `.env` to source control.
